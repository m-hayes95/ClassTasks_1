using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Task3 : MonoBehaviour
{
    //try list or dictionary

    private void Start()
    {
        
    }
    private void Update()
    {
        

              /*  if (i == 0 && j == 0)
                {
                    int plus = i + j;
                    Debug.Log("plus = " + plus);
                }
                if (i == 1 && j == 1)
                {
                    int minus = i - j;
                    Debug.Log("minus = " + minus);
                }
                if (i == 2 && j == 2)
                {
                    int multiply = i * j;
                    Debug.Log("multiply = " + multiply);
                }
                if (i == 3 && j == 3)
                {
                    int divide = i / j;
                    Debug.Log("divide = " + divide);
                } */
    }
}
    

