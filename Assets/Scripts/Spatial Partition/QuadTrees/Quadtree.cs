using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Quadtree : MonoBehaviour
{
    public QuadtreeNode root; // Ref to root of the Quadtree.
    public int maxObjectsPerNode; // The max number of objects the Quadtree can hold.
    public int maxDepth; // The max depth of the Quadtree.

    // Insert a game obbject into the Quadtree.
    public void Insert(GameObject obj)
    {
       
    }
}
